<nav class="navbar navbar-expand-lg navbar-light bg-warning" id="app-navbar">
    <div class="container-fluid">
        <ul class="navbar-nav">
            <li class="navbar-brand">
                <img src="../../static/images/pokeball.png" style="height:25px;width:25px;">
            </li>
            <li class="nav-item">
                <a class="nav-link active" aria-current="page" href="{{url_for('main.index')}}">Home</a>
            </li>
            {% if current_user.is_authenticated %}
            <li class="nav-item">
                <a class="nav-link active" href="{{url_for('main.team')}}">Team</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="{{url_for('main.opponent')}}">Opponent</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="{{url_for('main.results')}}">Results</a>
            </li>
            {% endif %}
        </ul>
        <ul class="navbar-nav ms-auto align items-center">
            {% if current_user.is_authenticated %}
            <li class="nav-item">
                <a class="nav-link active" href="{{url_for('auth.edit_profile')}}">Edit Profile</a>
            </li>
            <li class="nav-item">
                <a class="nav-link active" href="{{url_for('auth.logout')}}">Logout</a>
            </li>
            {% else %}
            <li class="nav-item">
                <a class="nav-link active" href="{{url_for('auth.login')}}">Login</a>
            </li>
            {% endif %}
        </ul>
    </div>
</nav>
